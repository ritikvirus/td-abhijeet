# from flask import Flask
# from flask_cors import CORS  # Import CORS
# from app.routes.aws_routes import aws_bp  # Import the blueprint

# # Initialize the Flask app
# app = Flask(__name__)

# # Configure CORS for the entire app or specific routes
# CORS(app)  # Enable CORS for all routes

# # Register the blueprint with '/api/v1/instance' prefix
# app.register_blueprint(aws_bp, url_prefix='/api/v1/instance')


